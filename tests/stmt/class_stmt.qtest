%%describe
Supports formatting classes
%%source
class Walkable
walk(steps)
join( *other)
    bang!
    not_bang()
multiple_args(a,b,*c)
end
%%expect
class Walkable
  walk( steps )
  join( *other )
  bang!
  not_bang()
  multiple_args( a, b, *c )
end
