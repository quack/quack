%%describe
Supports formatting classes
%%source
class Walkable
fn walk(steps)
fn join( *other)
fn join(...*rst)
end
%%expect
class Walkable
  fn walk( steps )
  fn join( *other )
  fn join( ... *rst )
end
