%%describe
Supports formatting traits
%%source
trait Walkable
fn walk(steps)
fn join( *other)
fn join(...*rst)
end
%%expect
trait Walkable
  fn walk( steps )
  fn join( *other )
  fn join( ... *rst )
end
